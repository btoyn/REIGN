import { NotBuiltYet } from "@/components/NotBuiltYet";

export default function ProgressPage() {
  return (
    <NotBuiltYet title="Progress">
      History, personal records and how your lifts are trending. All of it is
      calculated from logged workouts, so it stays empty until there are some.
    </NotBuiltYet>
  );
}
