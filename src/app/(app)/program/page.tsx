import { NotBuiltYet } from "@/components/NotBuiltYet";

export default function ProgramPage() {
  return (
    <NotBuiltYet title="Program">
      Your weekday split lives on Today and needs nothing here. This tab is for
      browsing real programs, which is a later milestone. Following one will
      always be optional.
    </NotBuiltYet>
  );
}
