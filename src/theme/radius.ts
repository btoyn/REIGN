/**
 * Corner radii.
 *
 * Source of truth: docs/design/REIGN_UI_SPEC.md ("Corner Radius").
 * Moderate by design — no pill shapes throughout the interface.
 */
export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
} as const;
